apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: http-vs
spec:
  hosts:
    - "*"
  gateways:
    - http-gateway
  http:
  - route:
    - destination:
        port:
           number: 8888 
        host: ping.default.svc.cluster.local
        subset: v1
